{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "supabase": {
      "type": "local",
      "command": ["cmd", "/c", "npx", "-y", "@supabase/mcp-server-supabase@latest", "--project-ref=atodbelpainjiqhgoese"],
      "enabled": true,
      "env": {
        "SUPABASE_ACCESS_TOKEN": "${env:SUPABASE_ACCESS_TOKEN}"
      }
    }
  }
}
